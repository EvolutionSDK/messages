message:
	singular: message
	plural: messages
	fields:
		namespace: string
		message: string
		type: string
		field: string
		value: string
		status: "enum('active','to_clear','cleared')"
		viewed: "enum('no','yes')"
		$session_id: string
	hasOne:
		- members.account